export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4">
      <h1 className="text-2xl font-semibold">Outbox Scheduler</h1>
      {/* TODO: replace with NextAuth "Sign in with Google" button, redirect to /dashboard */}
    </main>
  );
}
