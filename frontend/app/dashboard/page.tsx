// TODO: assemble DashboardHeader + tabs (Scheduled / Sent) + ComposeModal here.
export default function DashboardPage() {
  return (
    <main className="p-6">
      <h1 className="text-xl font-semibold">Dashboard</h1>
    </main>
  );
}
