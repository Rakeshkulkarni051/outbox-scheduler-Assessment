# Outbox Scheduler

Full-stack email job scheduler — Express + BullMQ + Redis + Postgres backend,
Next.js + Tailwind frontend.

## Architecture
See `/backend/ARCHITECTURE.md` (added as the feature work lands) for scheduling,
persistence, and rate-limiting design.

## Setup
See root-level setup steps in the project chat / SETUP.md.
